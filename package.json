{
	"name": "cftrojan-tunnel-worker",
	"version": "0.2.1",
	"private": true,
	"type": "module",
	"scripts": {
		"deploy": "npm run build && wrangler deploy _worker.js",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"build": "node build.js"
	},
	"devDependencies": {
		"esbuild": "^0.25.8",
		"terser": "^5.43.1"
	}
}
